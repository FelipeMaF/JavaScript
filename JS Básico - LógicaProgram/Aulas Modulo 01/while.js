let velocidade = 80

while (velocidade > 0) {
    alert ("O carro está a " + velocidade + "km/h")
    velocidade -= 20
    alert("Diminuindo 20 km/h")

    /* Abaixo exemplo de if dentro do while
    if (velocidade === 40) {
        break
    */
}

alert ("O carro parou.")